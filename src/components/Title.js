import styles from "../style/Title.module.scss";
const Title = () => {

    return (
        <h1 className={styles.backgroundTitle}>
            KARNAUGH MAP
        </h1>
    )
}

export default Title;